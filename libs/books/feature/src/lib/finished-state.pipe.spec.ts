import { FinishedStatePipe } from './finished-state.pipe';

describe('FinishedStatePipe', () => {
  it('create an instance', () => {
    const pipe = new FinishedStatePipe();
    expect(pipe).toBeTruthy();
  });
});
